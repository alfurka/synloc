[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "synloc"
version = "1.0.0"
authors = [
    {name = "Ali Furkan Kalay", email = "alfurka@gmail.com"},
]
description = "A Python package to create synthetic data from locally estimated distributions"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "numpy",
    "scipy",
    "matplotlib",
    "scikit-learn",
    "joblib",
    "tqdm"
]

keywords = [
    "copulas",
    "distributions",
    "sampling",
    "synthetic-data",
    "oversampling",
    "nonparametric-distributions",
    "semiparametric",
    "nonparametric",
    "knn",
    "clustering",
    "k-means",
    "multivariate-distributions"
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent"
]
license = {text = "MIT"}

[project.optional-dependencies]
dev = [
    "pytest",
    "sphinx",
]
docs = [
    "sphinx",
]

[project.urls]
Homepage = "https://github.com/alfurka/synloc"
Documentation = "https://alfurka.github.io/synloc/"
